# Never put real values in this file. Copy it to .env outside Git tracking.
RESCUEMESH_API_TOKEN_FILE=runtime/secrets/api.token
RESCUEMESH_MASTER_KEY_FILE=runtime/secrets/master.key
RESCUEMESH_SIGNING_PRIVATE_KEY_FILE=runtime/secrets/signing-private.pem
RESCUEMESH_SIGNING_PUBLIC_KEY_FILE=runtime/secrets/signing-public.pem

# Deliberately absent: the mainnet acknowledgement cannot live in a file.
